[project]
name = "Lemma"
version = "0.1.0"
description = "Use declarative way to generate pics"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.2.0",
    "pyyaml>=6.0.3",
]
